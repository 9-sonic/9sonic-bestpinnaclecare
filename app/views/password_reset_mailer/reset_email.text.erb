Hi <%= @name %>,

We received a request to reset your Best Pinnacle Care password.

Reset it here (link valid for <%= @expiry_hrs %> hours):
<%= @reset_url %>

If you didn't request this, you can safely ignore this email — your password won't change.
