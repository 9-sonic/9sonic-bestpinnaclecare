# Shared Contracts

TypeScript interfaces and API schemas shared by `/pwa`, `/admin-web`, and `/backend`.

**PM note:** Any PR that changes `/contracts` without a matching consumer change in PWA/admin/backend is a red flag — the handshake between carer and manager apps may be broken.
