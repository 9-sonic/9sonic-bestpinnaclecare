# Carer PWA

Mobile progressive web app for carers: offline-first clock-in/out, GPS capture, and shift dashboard.

Owned primarily by FE. The API contract is the generated OpenAPI doc (`swagger/`, served at `/api-docs`).
