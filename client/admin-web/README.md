# Manager Admin Web

Manager website: live board, late/missed alerts, timesheet approval, and CSV/Excel export.

Owned by FE/BE. The API contract is the generated OpenAPI doc (`swagger/`, served at `/api-docs`).
